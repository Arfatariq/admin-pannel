import 'package:flutter/material.dart';

class MyAppColors {
  static const Color primary = Colors.orange; // Main theme color
  static const Color secondary = Colors.white; // Secondary background or text
  static const Color backgroundColor =
      Color(0xFFFDF6F0); // Soft background (light orange/white tone)
  static const Color iconColor = Colors.deepOrange; // For icons
  static const Color textColor = Colors.black87; // Primary text color
  static const Color buttonColor = Colors.orangeAccent; // Button accent color
  static const Color borderColor = Color(0xFFE0E0E0); // Light grey for borders
}
